var lotteryNumbers = [4, 67, 32, 99, 44]
var babyNames = ["Maddy", "Kate", "Payton"]
var trueOrFalse = [true, false]

// Removes the last element of the babyNames array
babyNames.pop()

// Checks if the lotteryNumbers array includes the number 44 and returns a boolean
lotteryNumbers.includes(44)

// Returns a new array containing the second and third element from the lotteryNumbers array
lotteryNumbers.slice(2, 4)